From: Daniel van Vugt <daniel.van.vugt@canonical.com>
Date: Tue, 17 Sep 2024 15:52:52 +0800
Subject: cogl: Restore support for EGL 1.4 and GLES 2.0

This is required for Xilinx (Mali) hardware support.

Origin: https://gitlab.gnome.org/GNOME/mutter/-/merge_requests/4019
Bug-Ubuntu: https://bugs.launchpad.net/bugs/2080602
---
 cogl/cogl/driver/gl/gl/cogl-driver-gl.c     | 20 ++++++++++++--------
 cogl/cogl/driver/gl/gles/cogl-driver-gles.c |  7 ++-----
 cogl/cogl/winsys/cogl-winsys-egl.c          |  7 ++++++-
 3 files changed, 20 insertions(+), 14 deletions(-)

diff --git a/cogl/cogl/driver/gl/gl/cogl-driver-gl.c b/cogl/cogl/driver/gl/gl/cogl-driver-gl.c
index ef962ae..06a7fb9 100644
--- a/cogl/cogl/driver/gl/gl/cogl-driver-gl.c
+++ b/cogl/cogl/driver/gl/gl/cogl-driver-gl.c
@@ -468,14 +468,6 @@ _cogl_driver_update_features (CoglContext  *ctx,
   ctx->glGetString =
     (void *) cogl_renderer_get_proc_address (ctx->display->renderer,
                                              "glGetString");
-  ctx->glGetStringi =
-    (void *) cogl_renderer_get_proc_address (ctx->display->renderer,
-                                             "glGetStringi");
-  ctx->glGetIntegerv =
-    (void *) cogl_renderer_get_proc_address (ctx->display->renderer,
-                                             "glGetIntegerv");
-
-  gl_extensions = _cogl_context_get_gl_extensions (ctx);
 
   if (!check_gl_version (ctx, error))
     return FALSE;
@@ -487,6 +479,18 @@ _cogl_driver_update_features (CoglContext  *ctx,
   if (!check_glsl_version (ctx, error))
     return FALSE;
 
+  /* These are only used in _cogl_context_get_gl_extensions for GL 3.0
+   * so don't look them up before check_gl_version()
+   */
+  ctx->glGetStringi =
+    (void *) cogl_renderer_get_proc_address (ctx->display->renderer,
+                                             "glGetStringi");
+  ctx->glGetIntegerv =
+    (void *) cogl_renderer_get_proc_address (ctx->display->renderer,
+                                             "glGetIntegerv");
+
+  gl_extensions = _cogl_context_get_gl_extensions (ctx);
+
   if (G_UNLIKELY (COGL_DEBUG_ENABLED (COGL_DEBUG_WINSYS)))
     {
       g_autofree char *all_extensions = g_strjoinv (" ", gl_extensions);
diff --git a/cogl/cogl/driver/gl/gles/cogl-driver-gles.c b/cogl/cogl/driver/gl/gles/cogl-driver-gles.c
index 3733edf..f21d51d 100644
--- a/cogl/cogl/driver/gl/gles/cogl-driver-gles.c
+++ b/cogl/cogl/driver/gl/gles/cogl-driver-gles.c
@@ -688,11 +688,6 @@ _cogl_driver_update_features (CoglContext  *context,
   context->glGetString =
     (void *) cogl_renderer_get_proc_address (context->display->renderer,
                                              "glGetString");
-  context->glGetStringi =
-    (void *) cogl_renderer_get_proc_address (context->display->renderer,
-                                             "glGetStringi");
-
-  gl_extensions = _cogl_context_get_gl_extensions (context);
 
   if (!check_gl_version (context, error))
     return FALSE;
@@ -704,6 +699,8 @@ _cogl_driver_update_features (CoglContext  *context,
   if (!check_glsl_version (context, error))
     return FALSE;
 
+  gl_extensions = _cogl_context_get_gl_extensions (context);
+
   if (G_UNLIKELY (COGL_DEBUG_ENABLED (COGL_DEBUG_WINSYS)))
     {
       g_autofree char *all_extensions = g_strjoinv (" ", gl_extensions);
diff --git a/cogl/cogl/winsys/cogl-winsys-egl.c b/cogl/cogl/winsys/cogl-winsys-egl.c
index 7a9d6cb..503fd86 100644
--- a/cogl/cogl/winsys/cogl-winsys-egl.c
+++ b/cogl/cogl/winsys/cogl-winsys-egl.c
@@ -110,7 +110,12 @@ static GCallback
 _cogl_winsys_renderer_get_proc_address (CoglRenderer *renderer,
                                         const char   *name)
 {
-  return eglGetProcAddress (name);
+  GCallback result = eglGetProcAddress (name);
+
+  if (result == NULL)
+    g_module_symbol (renderer->libgl_module, name, (gpointer *)&result);
+
+  return result;
 }
 
 static void
